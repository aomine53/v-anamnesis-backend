server:
  port: 8002

spring:
  application:
    name: PRESCRIPTION-SERVICE
  datasource:
    password: Sam@1910
    url: jdbc:postgresql://localhost:5432/PrescriptionService
    username: postgres
  jpa:
    open-in-view: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true